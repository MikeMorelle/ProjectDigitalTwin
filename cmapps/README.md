Um das Projekt zu starten, öffne ein Terminal und...

1. erstelle ein virtual env:

- python -m venv vm

2. aktiviere Umgebung:

- .\vm\Scripts\activate

3. lade benötigte Dependencies runter:

- pip install -r requirements.txt

4. führe st aus:

- streamlit run app.py
